// This is the "ShapesRelate" interface, which defines a single method for comparing shapes.
public interface ShapesRelate {
    // This method compares shapes based on their relationship and returns an integer result.
    public int compareShapes(ShapesRelate shapesRelate);
}
